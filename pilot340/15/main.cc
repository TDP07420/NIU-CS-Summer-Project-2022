#include <iostream>
#include <fstream>
#include <sstream>
#include <string>

using std::cin;

int main(int argc, char *argv[])
{
	//declare input stream
	std::ifstream infile
	
	while(cin)
	{
		getline(cin,input);
		std::cout << input << std::endl;
	}
	
	exit(0);
}
